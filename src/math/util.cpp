#include "math/util.hpp"

namespace math {
  const Float PI              = MATH_PI;
  const Float TWO_PI          = 2.f * MATH_PI;
  const Float FOUR_PI         = 4.f * MATH_PI;
  const Float INV_PI          = 1.f / MATH_PI;
  const Float INV_TWO_PI      = 1.f / (2 * MATH_PI);
  const Float INV_FOUR_PI     = 1.f / (4 * MATH_PI);
  const Float INV_EIGHT_PI    = 1.f / (8 * MATH_PI);
  const Float PI_OVER_TWO     = MATH_PI / 2.f;
  const Float PI_OVER_FOUR    = MATH_PI / 4.f;
  const Float SQRT_TWO        = std::sqrt(2.f);
  const Float ONE_OVER_THREE  = 1.f / 3;
}
